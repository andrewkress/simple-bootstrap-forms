sudo: required

language: ruby

branches:
  only:
  - master

before_install:
  - mkdir ~/.gem
  - openssl aes-256-cbc -K $encrypted_7027482564bf_key -iv $encrypted_7027482564bf_iv -in credentials.enc -out ~/.gem/credentials -d

install:
  - echo "install nothing!"

script:
  - gem build simple-bootstrap-forms.gemspec

deploy:
  provider: script
  script: gem push *.gem
  on:
    branch: master
